# Building the Project

<!-- Add: install steps, build commands, env setup, local run instructions, and any build-time gotchas. For per-environment config (staging, prod), see deployment_workflow.md. -->

## Prerequisites

<!-- e.g. Node/npm or pnpm version, Docker if needed, other required tooling -->

## Install

<!-- e.g. npm install, pnpm install -->

## Build

<!-- e.g. npm run build -->

## Environment

<!-- Required env vars and where to set them (e.g. .env.example) -->

## Local development

<!-- How to run the app locally: dev server, ports, hot reload -->
