# Code Conventions

<!-- Style, typing, structure, naming, imports, comments. Testing: testing.md. -->

**Principles:** Use env vars for secrets (never hardcode). Write tests for core logic. Prefer small, focused files and single responsibility. Prefer explicit types over inference where it aids readability.

## Structure & naming

<!-- Folder layout (e.g. by feature vs by type), file naming (PascalCase components, camelCase utils, kebab-case?), component/module size guidelines. -->

## Imports

<!-- Import order (stdlib → external → internal), path style (aliases, no relative deep paths?), barrel files yes/no. -->

## Comments & docs

<!-- When to comment (why, not what); JSDoc or equivalent for public APIs; TODO/XXX conventions. -->
