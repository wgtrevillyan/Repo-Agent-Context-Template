# Deployment Workflow

<!-- Add: CI/CD pipeline, env vars per environment, feature flags. Local env setup: building_the_project.md. -->

## CI/CD

<!-- Branch strategy (e.g. main = prod, develop = staging), pipeline stages, what runs on push/PR, deploy triggers -->

## Environment variables

<!-- Which vars by environment (dev, staging, prod), where they are set (e.g. Vercel, GitHub Secrets). Local: building_the_project.md. -->

## Feature flags

<!-- How flags are managed and toggled per environment or cohort -->
