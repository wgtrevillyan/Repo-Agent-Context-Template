# Database Schema & Patterns

<!-- Add: tables, key columns, relationships, query patterns, migrations, seeding. -->

## Tables

<!-- e.g. letter_issues, users_info, and others (see ../web-app/lib/backend/supabase/database/ for v1 reference) -->

## Key relationships

<!-- How tables link together (FKs, cardinality) -->

## Query patterns

<!-- Preferred ways to query (e.g. via Supabase client, raw SQL), N+1 avoidance, indexing expectations -->

## Migrations

<!-- Where schema changes live and how to apply them -->

## Seeding

<!-- When seeding runs (e.g. manual script, post-migrate, test setup) and what seed data exists -->
